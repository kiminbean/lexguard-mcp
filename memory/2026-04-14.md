# 2026-04-14 (화) — 작업 로그

## voice-to-textnote 보안 감사 + 파이프라인 버그 수정 (Codex)

### 보안 커밋: `3880788`
- 타이밍 공격 방지 (`secrets.compare_digest`)
- JWT 시크릿 자동 생성 + 프로덕션 보안 강제
- FTS5 검색 인젝션 방지
- `datetime.utcnow()` deprecated 해결
- 설정 값 범위 검증, 빈 파일 업로드 차단

### 파이프라인 버그 수정 커밋: `57195c0`
- 12개 파일, 322줄 추가 / 81줄 삭제
- **핵심 원인 (15분+ 에러)**:
  1. 화자 분리 OOM — `torchaudio.load()` 전체 로드 → 청크별 `frame_offset`/`num_frames` 부분 로드로 변경
  2. 15분 경계 오프바이원 — `> threshold` → `>= threshold`
  3. 오버랩 세그먼트 잘못 폐기 → 오버랩 구간만 잘라내고 나머지 유지
- 파이프라인 안정성: 선행 STT/DIA 실패 무시 문제, 재시도 시 원본 삭제, 화자 매칭 알고리즘 재작성, 메모리 누수, 오디오 길이 측정 개선, 인접 세그먼트 미병합
- API: 실패 시 `error_message` 필드 일관 반환

## web_fetch_js — SPA 렌더링 웹 스크래퍼 신규 제작
- `web_fetch` 도구가 JS 렌더링 불가 → Playwright 기반 스크립트 자체 제작
- 경로: `scripts/web_fetch_js` (래퍼) → `scripts/web-fetch-js/web_fetch_js.mjs`
- 의존: Playwright (scripts/web-fetch-js/node_modules에 독립 설치)
- ClawHub 등 SPA 페이지 정상 렌더링 확인 완료
- HTML→Markdown 변환은 pure JS (외부 의존 없음)

## 사용자 지시사항
- 문제 발생 시 지시하지 않아도 Claude Code/Codex와 협업해서 도구/해결책을 능동적으로 만들 것
- USER.md에 작업 스타일로 기록함

### Codex 세션
- `cool-bison` 세션 사용
- 모델: gpt-5.4 (Codex v0.118.0)
- 이후 추가 버그픽스 실행 중 (`cool-bis` 세션) — STT 실패 시 화자 분리용 WAV 정리 로직 등
- 추가 점검 세션 `grand-breeze` 실행 중 (동시성, 에러 핸들링, 보안 등)
