# 2026-04-04 Daily Notes

## 🧠 학습 루프 구현 (옵션 A)

### 구현 내용
Hermes Agent의 자기 학습 개념을 OpenClaw에 적용

**1. 학습 상태 추적** — `memory/learning-state.json`
- 스킬 생성/개선 횟수, 패턴 감지, 사용자 모델 업데이트 추적

**2. 자동 스킬 생성** — `scripts/auto-skill.sh`
- 반복 작업 패턴 감지 시 자동으로 스킬 스캐폴드 생성
- 이미 존재하면 개선 모드로 전환
- 학습 상태 자동 업데이트

**3. HEARTBEAT 학습 루프** — 5단계 추가
- 6. 패턴 감지 및 스킬 자동 생성
- 7. 사용자 모델 자동 업데이트 (USER.md)
- 8. 스킬 자기 개선 (사용 이력 기반)
- 9. 학습 메트릭 기록

**4. 사용자 모델 강화** — `USER.md`
- 관심사, 작업 패턴, 명시적 피드백, 커뮤니케이션 스타일 추적
- 학습 루프에 의해 자동 업데이트

**5. MEMORY.md 학습 메트릭 섹션** 추가

### Hermes Agent → OpenClaw 매핑
| Hermes 기능 | OpenClaw 구현 |
|---|---|
| 스킬 자동 생성 | auto-skill.sh + HEARTBEAT 루틴 |
| 스킬 자기 개선 | HEARTBEAT 8번 항목 |
| 사용자 모델링 | USER.md + HEARTBEAT 7번 항목 |
| 세션 간 기억 | memory-core + MEMORY.md + knowledge-base.md |
| 지식 자동 저장 | HEARTBEAT 1~4번 항목 |

## 🔧 시스템 변경
- 비활성화된 플러그인 삭제 (memory-lancedb, perplexity, amazon-bedrock)
- memory 슬롯: memory-core로 변경

## 📋 크론잡 권한 수정
- AI Marketer 데일리 점검: exec 권한 추가
- 타로 심리상담 데일리 점검: exec 권한 추가
# 2026-04-04 Daily Notes

## 🧠 학습 루프 구현 (옵션 A)

### 구현 내용
Hermes Agent의 자기 학습 개념을 OpenClaw에 적용

**1. 학습 상태 추적** — `memory/learning-state.json`
- 스킬 생성/개선 횟수, 패턴 감지, 사용자 모델 업데이트 추적

**2. 자동 스킬 생성** — `scripts/auto-skill.sh`
- 반복 작업 패턴 감지 시 자동으로 스킬 스캐폴드 생성
- 이미 존재하면 개선 모드로 전환
- 최대 50개 제한, 한국어 이름에 `ko-` prefix
- 학습 상태 자동 업데이트

**3. HEARTBEAT 학습 루프** — 12개 루틴 (3단계 실행 빈도)
- ⚡ 매 Heartbeat: 이상 탐지만 — 정상이면 HEARTBEAT_OK
- 📍 일 1회: 지식 정리, 인사이트, 보안, 메트릭
- 🧠 주 1회 (일요일): 패턴 감지, 스킬 생성, 예측 검증, 크론잡 감사

**4. 사용자 모델 강화** — `USER.md`
- 관심사, 작업 패턴, 명시적 피드백, 커뮤니케이션 스타일 추적

**5. MEMORY.md 학습 메트릭 섹션** 추가

### 추가 구현 (5가지 개선)
1. **오류 기반 학습** — `memory/failure-patterns.md` (6개 과거 실패 기록)
2. **크론잡 성과 추적** — `scripts/cron-audit.sh` (텍스트 출력 파싱)
3. **예측 정확도 추적** — `scripts/prediction-tracker.sh`
4. **세션 간 지식 전달** — AGENTS.md Session Startup 강화
5. **작업 후 자동 학습** — AGENTS.md에 규칙 추가

### Hermes Agent → OpenClaw 매핑
| Hermes 기능 | OpenClaw 구현 |
|---|---|
| 스킬 자동 생성 | auto-skill.sh + HEARTBEAT 루틴 |
| 스킬 자기 개선 | HEARTBEAT 8번 항목 |
| 사용자 모델링 | USER.md + HEARTBEAT 7번 항목 |
| 세션 간 기억 | memory-core + MEMORY.md + knowledge-base.md |
| 지식 자동 저장 | HEARTBEAT 1~4번 항목 |

## 🔧 시스템 변경
- 비활성화된 플러그인 삭제 (memory-lancedb, perplexity, amazon-bedrock)
- memory 슬롯: memory-core로 변경

## 📋 크론잡 권한 수정
- AI Marketer 데일리 점검: exec 권한 추가
- 타로 심리상담 데일리 점검: exec 권한 추가

## 🔒 점검 및 안정화
- 학습 루프 전체 점검 완료
- 토큰 과다 소모 방지: HEARTBEAT를 3단계 실행 빈도로 분리
- 스킬 범람 방지: 최대 50개 제한
- 스크립트 실행 권한 (chmod +x) 부여
- 한국어 스킬 이름에 `ko-` prefix 추가
- cron-audit.sh: --json 대신 텍스트 파싱으로 변경

## 🌤️ 날씨
- 오전: 비 (14°C), 오후: 비 멈춤
- 내일(일): 낮 17°C 맑음, 밤 11°C로 급강하 — 겉옷 필요
