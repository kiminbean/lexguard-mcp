```diff
--- a/scripts/notify-hyperagent.sh
+++ b/scripts/notify-hyperagent.sh
@@ -56,6 +56,21 @@ send_telegram() {
}
 
 # ─── 메인: state에서 결과 추출 → 메시지 조립 ───
+retry_send_telegram() {
+    local message="$1"
+    local retries=3
+    local delay=5
+
+    for ((i=0; i<retries; i++)); do
+        if send_telegram "$message"; then
+            return 0
+        fi
+        echo "전송 실패, 재시도 중... ($((i+1))/$retries)"
+        sleep "$delay"
+    done
+
+    return 1
+}
+
 compose_and_send() {
     if [ ! -f "$STATE_FILE" ]; then
         log_event "no-state"
@@ -81,7 +96
     log_event "started" "\"msgLen\":${#message}"
-    if send_telegram "$message"; then
+    if retry_send_telegram "$message"; then
         echo '{"status":"delivered"}'
         return 0
     else
```
