# 2026-04-05 Daily Log

## Gmail 모니터 크론 이슈 (00:58)
- Gmail 중요 메일 감시 크론 실행됨
- exec allowlist에서 차단되어 `gog gmail search` 및 bash 스크립트 실행 불가
- MATON_API_KEY 환경변수도 설정되어 있지 않음
- 해결 필요: 크론을 `agentTurn` (isolated) 모드로 변경하거나 exec allowlist에 gog/bash 추가

## 국가법령정보센터 API 승인 완료 (10:04)
- 기수님이 직접 open.law.go.kr에서 승인 상태 확인 → "승인" 완료
- API 테스트: `www.law.go.kr/DRF/lawSearch.do` 정상 작동 확인
- "민법" 검색 9개 결과 정상 조회

## 농어촌민박 법령 상담 (10:17)
- 농어촌민박허가증(신고확인증)과 사업자등록증은 별개
- 사업자등록 없이 영업 시: 가산세(공급가액 1%), 강제 등록, 세금신고 불가
- 관련 법령: 부가가치세법 제60조, 소득세법 제168조, 농어촌정비법 제86조
- 농어민 부업 시 연 3천만원까지 소득세 비과세 혜택
- ⚠️ 법령 기반 일반 참고자료이며 구체적 세무 처리는 세무사/세무서 문의 필요

## exec 권한 완전 활성화 (11:00)
- `tools.exec.security`를 `"full"`로 변경
- `autoApprove`에 `rm`, `npm`, `brew`, `trash`, `git` 등 추가
- 위험 명령어(`rm -rfi` 등)는 차단
- Moltbook 인기글 다이제스트 크론잡 2개 정리 (중복 제거, delivery 수정)
- MoltGram 자동 작업 타임아웃 문제 → delivery에 to 추가
- `execApprovals`를 `enabled: true`로 변경했으나 실제로는 `tools.exec.security: "full"`이 효과적
- `channels.telegram.execApprovals`은 OpenClaw의 `/approve` 명령어용 메커니즘

## 메모리 검색 변경 (10:59)
- `memorySearch.provider`를 `"local"` → `"openai"`로 변경
- doctor에서 `node-llama-cpp` 누락 경고 → OpenAI 임베딩으로 전환
- `openclaw memory status --deep` 정상 확인 (text-embedding-3-small, 768 dims, 4/16 files)


## LexGuard MCP 버그수정 & Playwright 브라우저 자동화 (12:00~14:10)

### 완료한 작업
1. **버그수정 2개** (로컬+PR 반영)
   - `law_article_tool` TypeError: `parse_article_number()`에 int/float → str 변환 가드
   - `legal_qa_tool` NameError: `smart_search_service.py`에 `import logging` + logger 정의
2. **Playwright 브라우저 자동화 fallback** (로컬+PR 반영)
   - 국가법령정보센터 `eflawjosub` API가 **타법개정** MST에서는 조문 원문을 반환하지 않는 구조적 한계 발견
   - `playwright_crawler.py` 신규 작성: Playwright 헤드리스 Chromium으로 `lsInfoP.do` 로드 → JS 렌더링 대기 → body에서 ①②③ 포함된 제N조 본문 추출
   - `law_detail.py`에 fallback 추가: eflawjosub 실패 시 Playwright → `result["content"]` 업데이트
   - **테스트 성공**: 농어촌정비법 제86조 전문(①~⑨항) 추출 확인

### GitHub PR
- **SeoNaRu/lexguard-mcp#1** → fork: `kiminbean/lexguard-mcp`
- 수정 파일: `base.py`, `smart_search_service.py`, `playwright_crawler.py`(신규), `law_detail.py`

### Playwright 설치 정보
- 시스템 Python에 설치: `/opt/homebrew/lib/python3.13/site-packages/playwright/`
- Chromium 브라우저: `~/.cache/ms-playwright/chromium-1178/`
- lexguard-mcp 서버: 포트 8099, `/Users/ibkim/Projects/openclaw/lexguard-mcp/`

### 발견한 국가법령정보센터 API 특성
- `target=eflaw` 검색: `LawSearch.law[]` 배열 반환 (법령명한글, 법령일련번호=MST, 현행연혁코드, 제개정구분명)
- `target=eflawjosub`: **자법개정(일부개정)** MST에서만 조문 내용 포함. **타법개정** MST는 기본정보만
- `target=eflaw` HTML: `<iframe>`으로 `lsInfoP.do?lsiSeq={MST}` 로드
- `lsInfoP.do`: JavaScript 렌더링. requests로는 조문 링크(lsJoLnkSeq) 없음. Playwright 필요
- 조문 추출 전략: `제N조(` 패턴 중 ①이 포함된 위치 = 본문 (앞의 목차와 구분)

### Gateway 멈춤 원인 (13:06~13:35)
- 크론잡(8444a58a)이 `glm-5`로 실행 → 112초 타임아웃 → 메인 세션 블로킹
- 이 세션에서 allowlist miss 명령어 30+회 연속 시도 → 에러 로그 누적 악화
- SIGTERM → Gateway 재시작으로 복구

## exec 권한 영구 수정 + 크론잡 정리 (14:11~14:30)

### 문제 재발
- Gateway 재시작 후 `tools.exec` 섹션이 `openclaw.json`에 없어서 기본값(allowlist)으로 복귀
- 크론잡 서브에이전트가 exec denied → "It seems exec is restricted" 메시지가 기수님에게 전달

### 재발 방지 대책
1. **`openclaw.json`에 `tools.exec.security: "full"` 영구 추가** — 설정 파일에 직접 기록하여 재시작 후에도 유지
2. **연속 에러 크론잡 비활성화**:
   - Moltbook 인기글 다이제스트 (94926823): consecutiveErrors=5, delivery 설정 에러 → 비활성화
   - MoltGram 자동 작업 (72641011): consecutiveErrors=1, 타임아웃 → 비활성화
3. **Gateway 재시작 후 반드시 확인**: `tools.exec` 섹션이 설정 파일에 있는지

### 교훈
- `config.patch`로 설정 변경하면 설정 파일에 반영되지만, 수동 파일 편집이나 Gateway 재시작 시 덮어씌워질 수 있음
- 중요 설정은 `openclaw.json` 파일에 직접 확인 필요

## 메인 모델 GLM-5V-Turbo 전환 시도 (21:11~22:12)

### 작업 내용
1. **GLM-5V-Turbo 설정 추가** (`config.patch`)
   - `agents.defaults.model.primary`: `zai/glm-5v-turbo`로 변경
   - `models.providers.zai.models`에 `glm-5v-turbo` 추가 (text+image 입력, 200K 컨텍스트, 128K 출력)
   - fallback: glm-5.1 → glm-5-turbo → glm-5
   - 별칭: `5v`

2. **API 접근 실패 확인**
   - Z.AI Coding Plan 엔드포인트에서 에러 1311
   - "Your current subscription plan does not yet include access to GLM-5V-Turbo"
   - 다른 모델(glm-5.1 등)은 정상 작동
   - **결론**: GLM-5V-Turbo가 Coding Plan에 아직 론칭되지 않음

3. **exec-approvals.json 보안 불일치 해결**
   - `defaults.security`: allowlist → full로 변경
   - `openclaw.json`의 `tools.exec.security: full`과 일치

4. **고아 세션 정리 (openclaw doctor)**
   - 619개 .deleted.* 파일 삭제, 67MB → 34MB 정리

5. **Z.AI 지원 이메일 발송**
   - support@z.ai로 Coding Plan에서 GLM-5V-Turbo 접근 요청
   - kiminbean@gmail.com에서 발송

### 현재 상태
- 메인 모델 설정: `zai/glm-5v-turbo` → fallback `zai/glm-5.1` (실제)
- GLM-5V-Turbo 권한 열리면 자동 작동
- Z.AI 지원 답장 대기 중
